import React, { Component } from 'react';

class BigTree extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div class="list-view" onScroll="handleScroll($event)">
                1111
            </div>
        )
    }
}

export default BigTree;